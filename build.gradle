

task purge(type:Delete) {
  //println 'Cleaning up old files'
  delete 'build'
}

task dependances(type:Exec, dependsOn: purge) {
	println 'hello'
  executable 'sh'
  args '-c', 'pip3 install flask'
  
   standardOutput = new ByteArrayOutputStream()
   ext.output = { return standardOutput.toString() }
}

def tarfile = "application-" + version

task packageDistribution(type: Zip, dependsOn: dependances) {
    from ('PYTHON') { into '.' }
    archiveFileName = tarfile + ".zip"
    destinationDirectory = file("build")
}

task version(type:Exec, dependsOn: packageDistribution) {
	workingDir '/var/lib/jenkins/workspace/GestionParcInformatique/PYTHON'
	commandLine 'python', 'Vesrion.py'

}


task up(type:Exec, dependsOn: version) {
 executable 'sh'

 args '-c', 'curl -v --user "admin:admin" --upload-file build/application-'+version+'.zip http://192.168.1.12:8081/repository/nexus-tpp/application-python/application-'+version+'.zip'
 standardOutput = new ByteArrayOutputStream()
 ext.output = { return standardOutput.toString() }
}
